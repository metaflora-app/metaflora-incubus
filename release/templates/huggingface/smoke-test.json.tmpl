{
  "schema_version": 1,
  "artifact_sha256": "${MODEL_SHA256}",
  "runtime": "${RUNTIME_ID}",
  "status": "${SMOKE_TEST_STATUS}",
  "request": "${SMOKE_TEST_REQUEST}",
  "response": "${SMOKE_TEST_RESPONSE}"
}
